<script lang="ts">import { page } from '$app/stores'; import Logo from '$lib/components/Logo.svelte'; $: code=$page.url.searchParams.get('code')??'DEMO'; let last='READY'; const press=(v:string)=>last=v;</script>
<div class="phone"><nav><Logo compact/><span>{code}</span></nav><main><p class="eyebrow">Controller connected</p><h1>{last}</h1><div class="preview"><span>YOUR PIECE</span><b>▦</b></div><div class="controls"><button aria-label="Move left" on:click={()=>press('LEFT')}>←</button><button aria-label="Rotate" class="rotate" on:click={()=>press('ROTATE')}>↻</button><button aria-label="Move right" on:click={()=>press('RIGHT')}>→</button><button aria-label="Soft drop" on:click={()=>press('DOWN')}>↓</button><button aria-label="Hard drop" class="drop" on:click={()=>press('DROP')}>DROP</button></div></main></div>
<style>.phone{width:min(100%,520px);margin:auto;padding:0 1rem;min-height:100vh}nav>span{font-weight:bold;color:var(--yellow)}main{text-align:center;padding-top:2rem}h1{font:900 2.4rem 'Arial Black';min-height:3rem}.preview{margin:1.5rem auto 2.5rem;width:150px;height:150px;background:#0a0b10;border:2px solid #343746;display:grid;place-items:center}.preview span{font-size:.65rem;color:var(--muted)}.preview b{font-size:4rem;color:var(--cyan)}.controls{display:grid;grid-template-columns:repeat(3,1fr);gap:.8rem}.controls button{height:80px;font-size:2rem;background:#292c38;color:var(--text)}.controls .rotate{background:var(--pink);color:var(--ink)}.controls .drop{grid-column:2/4;background:var(--cyan);color:var(--ink);font-size:1rem}</style>
