import{expect,test}from'@playwright/test';import{TestStepHelper}from'../helpers/test-step-helper';import{resetEmulators}from'../helpers/reset-emulators';test.beforeEach(resetEmulators);
test('US-005: Block Stack starts a deterministic playable controller',async({page},testInfo)=>{const tester=new TestStepHelper(page,testInfo);await page.goto('/');await page.getByLabel('Player name').fill('Nadia');await page.getByRole('button',{name:'Continue'}).click();await expect(page.getByRole('button',{name:'Play anonymously'})).toBeEnabled({timeout:30000});await page.getByRole('button',{name:'Play anonymously'}).click();await expect(page.getByText('ANONYMOUS PLAYER READY')).toBeVisible({timeout:10000});await page.getByRole('button',{name:'Create a room'}).click();await expect(page).toHaveURL(/room\?code=TEST/,{timeout:10000});await expect(page.getByRole('button',{name:/BLOCK STACK/})).toHaveClass(/chosen/);await page.getByRole('button',{name:'Play',exact:true}).click();await expect(page).toHaveURL(/\/tetris\?code=TEST/,{timeout:10000});const board=page.getByRole('img',{name:'Block Stack board'});await expect(board).toBeVisible({timeout:10000});await expect(board.locator('.filled')).not.toHaveCount(0);const spawnRow=await board.getAttribute('data-active-row');await expect.poll(()=>board.getAttribute('data-active-row'),{timeout:3000}).not.toBe(spawnRow);await page.keyboard.press('r');await page.keyboard.press('ArrowLeft');await page.keyboard.press('ArrowUp');await expect(page.getByText(/input\/hard-drop · tick/)).toBeVisible({timeout:10000});await page.locator('.command-status').evaluate((element:HTMLElement)=>{element.style.visibility='hidden'});await tester.step('tetris-playing',{description:'Block Stack runs from a seeded immutable command journal',networkStatus:'skip',verifications:[{spec:'The 10 by 20 play matrix is visible and gravity moves its active piece',check:async()=>{await expect(board.locator('i')).toHaveCount(200);await expect(board.locator('.filled')).not.toHaveCount(0)}},{spec:'Next queue and ghost placement are rendered locally',check:async()=>{await expect(page.getByLabel(/Next pieces/)).toBeVisible();await expect(board.locator('.ghost')).toHaveCount(4)}},{spec:'Movement, hard drop, and both SRS rotations are available',check:async()=>{await expect(page.getByRole('button',{name:'Move left'})).toBeEnabled();await expect(page.getByRole('button',{name:'Hard drop'})).toBeEnabled();await expect(page.getByRole('button',{name:'Rotate clockwise'})).toBeEnabled();await expect(page.getByRole('button',{name:'Rotate counterclockwise'})).toBeEnabled()}},{spec:'Score and line count start from deterministic state',check:async()=>{await expect(board).toHaveAttribute('data-lines','0');await expect(board).toHaveAttribute('data-score',/\d+/)}}]});tester.generateDocs()});
